paths:
  ping:
    get:
      tags:
        - system
      description: |
        ping then pong
      x-eov-operation-id: ping
      x-eov-operation-handler: ping
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string
                example: pong