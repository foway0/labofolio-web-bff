components:
  parameters:
    limitQueryParams:
      in: query
      name: limit
      schema:
        type: integer
        minimum: 1
        default: 20
        maximum: 100
    offsetQueryParams:
      in: query
      name: offset
      schema:
        type: integer
        minimum: 0
        default: 0

    userIdPathParams:
      in: path
      name: user_id
      schema:
        type: number
      required: true
    blogIdPathParams:
      in: path
      name: user_id
      schema:
        type: number
      required: true